# VerifiMind-PEAS MCP Server Dependencies
# Generated from pyproject.toml for CI/CD compatibility

# Core MCP — PINNED exactly (2026-07-06): trigger deploys re-resolve deps, and the
# 2026-07-28 MCP spec RC will land in SDKs soon; SDK bumps must be deliberate PRs.
# Versions = what prod (rev 00464) serves and 613 unit tests pass on.
mcp==1.28.1
fastmcp==3.4.4
starlette==1.3.1

# Data Validation
pydantic==2.13.4

# HTTP Client
httpx==0.28.1

# LLM Providers
openai>=1.0.0
anthropic>=0.5.0
google-genai>=1.0.0
groq>=0.4.0
mistralai>=1.0.0

# Configuration
python-dotenv>=1.0.0

# Server Framework
fastapi==0.139.0
uvicorn[standard]==0.51.0


# v0.5.6 Gateway: Early Adopter Registration + Privacy
google-cloud-firestore>=2.13.0
email-validator>=2.0.0

# v0.5.12 Polar Integration: Standard Webhooks signature verification
standardwebhooks>=1.0.0

# Testing (dev dependencies)
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
