# VerifiMind-PEAS MCP Server Dependencies — CI/dev MIRROR, not the authority.
#
# pyproject.toml is the DEPLOYMENT DEPENDENCY AUTHORITY: production's Dockerfile
# runs `uv pip install --system --no-cache .` and never reads this file. Editing
# only this file changes CI and does NOT change what ships.
#
# Every runtime line below must match pyproject.toml EXACTLY. This was previously
# asserted only by the header comment above, and had silently drifted on nine
# packages. It is now enforced by
# tests/unit/test_dependency_manifest_parity.py, which fails closed.
# Test-only extras live in the clearly marked final section and are the sole
# permitted difference.

# Core MCP — PINNED exactly (2026-07-06): trigger deploys re-resolve deps, and the
# 2026-07-28 MCP spec RC will land in SDKs soon; SDK bumps must be deliberate PRs.
# Versions = what prod serves and the full suite passes on.
mcp==1.28.1
fastmcp==3.4.6
starlette==1.4.1

# Data Validation
pydantic==2.13.4

# HTTP Client
httpx==0.28.1

# LLM Providers
openai>=1.0.0
anthropic>=0.94.0
google-genai>=1.0.0
groq>=1.1.2
mistralai>=1.0.0

# Configuration
python-dotenv>=1.2.2

# Server Framework
fastapi==0.141.1
uvicorn[standard]==0.52.1


# v0.5.6 Gateway: Early Adopter Registration + Privacy
google-cloud-firestore>=2.26.0
email-validator>=2.0.0

# v0.5.12 Polar Integration: Standard Webhooks signature verification
standardwebhooks>=1.0.0

# ---------------------------------------------------------------------------
# TEST-ONLY EXTRAS — the ONLY permitted difference from pyproject.toml.
# Anything added below is never installed into the production image.
# ---------------------------------------------------------------------------
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
